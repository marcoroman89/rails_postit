<h2>New Category</h2>

<%= render 'shared_partials/errors', object: @category %>

<%= form_for @category do |f| %>
  <%= f.label :name %><br/>
  <%= f.text_field :name %><br/>
  <br/>
  <%= f.submit "Create Category", class: 'btn btn-primary' %>
<% end %>
